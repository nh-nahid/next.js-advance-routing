import React from 'react';

const page = () => {
    return (
        <div>
            Parallel route is the process to use multiple page in a single page with individual loading, error
        </div>
    );
};

export default page;